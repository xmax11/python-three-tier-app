# Essential project identification
sonar.projectKey=python-three-tier-app
sonar.organization=xmax11

# Path to the source code (current directory)
sonar.sources=.

# Specifically for Python coverage (ensure this matches your pytest output)
sonar.python.coverage.reportPaths=coverage.xml

# Exclude tests from being analyzed as source code
sonar.exclusions=**/tests/**